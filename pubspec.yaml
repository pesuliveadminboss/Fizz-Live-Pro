name: pesulive
description: "A new Flutter project for social discovery, live streaming and calls."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.2   # 👈 Inga add pannu macha!
  # Add other existing packages below if any (zego_express_engine etc.)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # assets: ...
